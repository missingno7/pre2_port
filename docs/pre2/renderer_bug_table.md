# Faithful-renderer bug table (2026-06-24)

Built from the now-correct `--faithful-verify` signal (the 6772 frame-boundary `GameVisualState`
capture). At the boundary the camera/scroll false-positive is gone, so remaining diffs are real. Swept
the gameplay/transition witness set; ranked by impact.

| # | Witness / repro | Scene kind | Diff size / pattern | Affected layer | Classification | Root-cause hypothesis | Required fix | Type | Status |
|---|---|---|---|---|---|---|---|---|---|
| 1 | boss 192126/192140; gp 003317/010021 | GAMEPLAY | HUD Δ 92–250, cols **272–304** | HUD (BONUS box) | **renderer bug — missing leaf** | `draw_hud` omitted the collected B/O/N/U/S letters (glyphs 0x0C–0x10, ASM 46AD), gated by `[0x6CA7]`/flash `[0x6C00]` | recover the 46AD loop into `draw_hud` (same `blit_hud_glyph` leaf) | renderer bug | **FIXED — HUD Δ=0 all witnesses (8604ba1)** |
| 2 | gp 185902 Δ58, boss_192140 Δ332 (frame-dependent: 0 on non-blink frames) | GAMEPLAY | viewport ≤~330, a few sprite/boss-meter px | moving sprites | **state-feed timing (object blink)** | the object pass mutates each record's blink/life `[+0x11]` (gated `[0x6BD5]&3`) AS it draws, so the 6772 capture (post-pass) is one blink-phase off for a sprite that toggled this frame | optional: capture object records at the object-pass ENTRY (pre-mutation) instead of 6772; **NOT a renderer-completion blocker** (≤1% viewport, only on blink frames, imperceptible) | state-feed bug (minor) | documented |
| 3 | menu modeselect/menuredraw; map mapscroll | SCENE (0Dh) | full frame (raises `FaithfulVisualGap`) | scene compose | **scene gap — bg rebuild is the hard sub-piece** | TEXT (4 `draw_string` @9920..99A7), palette, cursor located. BUT the BACKGROUND is a **0x2000 circular-page window of an infinitely-scrolling master pattern** (seg `[0x2875]`), panned by the CRTC (`display_start=0x0397`, `scroll_x=0xFE74` on the witness). RE-TRACED (2026-06-24): the producer is **`scroll_blit_column` @965A — `mov ds,[0x2875]` confirmed the pattern source** (planar: per plane the src reads stride 0x4F over 200 rows, di stride 0x27, page wrap 0x1FFF) — but it fills only **ONE new column per 8-px boundary** (`test dl,7;jne`); the BULK of the page is maintained by **`scroll_shift_frame`'s 4-plane self-copy @9804** (shifts the buffer to follow the camera). So the menu page is a **STATEFUL scroll buffer** (like the gameplay scroll-ring) — replaying `scroll_blit_column` alone reproduces ~1% (the self-copy history is lost). `render_scene` takes rebuilt planes (NOT a VRAM copy). | The leaves (`scroll_blit_column`/`scroll_shift_frame`) are VERIFIED at their call sites (checkpoints/present.py). A from-scratch direct-sampling rebuild was attempted and **does NOT work**: scanning every page↔pattern offset `K` (`page[col+r*0x28] == pattern[(K+col+r*0x50)]`) reproduces only **~11%** (near-random) of plane 0. So the menu page is NOT a clean function of the current pattern+scroll — its content is **history-dependent** (the accumulated init fill + many `scroll_shift_frame` self-copies + per-column blits), and/or `[0x2875]` is not the live pattern at capture. Per the "stop if uncertain, don't guess a speculative scene builder" rule, `build_menu_background` is **BLOCKED on the buffer invariant**. Two grounded paths (future): (a) **replay** the menu scroll from a known init state (the game is deterministic) and snapshot at the commit boundary; or (b) recover the exact init-fill + self-copy invariant that maps the buffer to the pattern. | scene gap (multi-step) | **BLOCKED — no simple page↔pattern mapping (11% best). Documented; needs the buffer-invariant or a scroll-replay. Producer + leaves identified/verified; text/palette/cursor located.** |
| 4 | intro 163804; title 163923 | IMAGE (13h) | full frame (gap) | image | **scene gap — base LOCATED 2026-06-24, COMPOSITED** | the title 13h image is built by `1030:91A4`: set mode 13h, reset DAC to black, `call 0x926E` (ds=`[0x2DDA]+0x30`, the decoded-asset seg), `rep movsw` 0x7D00 words (64000 B) from ds:si → A000, then palette fade-in (`0x3034`). The base = **`PRESENT.SQZ`** decoded, offset 0x300 (past a 768-B palette) = the jungle/volcano BACKGROUND — **79% match** to the displayed frame. The remaining ~21% is the **"PREHISTORIK 2" logo + the animated "2"** composited on top (separate overlay, not yet located). So the image is COMPOSITED: PRESENT.SQZ bg + logo overlay. NOTE the frozen snapshot's `[0x2DDA]` is already reused for the level tilemap, so the source must be read live at 91C0, not from the snapshot. | (a) DONE: base = PRESENT.SQZ@0x300 via 91C0; (b) locate the logo/"2" overlay blit; (c) `render_image` leaf (decode PRESENT.SQZ linear + logo overlay) + bridge; (d) wire through `render_visual` IMAGE; (e) verify | scene gap (composited) | PARTIAL — base asset found (PRESENT.SQZ), logo overlay TBD |
| 4b | intro/title 13h — DEEPER (2026-06-24, hook-first; CORRECTED) | IMAGE (13h) | — | image codec | **CODEC IS ALREADY RECOVERED (correction) — pipeline located** | CORRECTION: the image decompressor is NOT a missing codec. `pre2/codecs/sqz.py unpack_sqz` already dispatches by signature and decodes all formats (`0x4CB4`→`unpack_sqz_lzss` = cyxx's "EAT"; `data[1]==0x10`→`unpack_sqz_lzw` = cyxx "SQZ"; else `unpack_sqz_other` = "SQV"). PRESENT.SQZ=LZW, MENU/GAMEOVER/SPRITES/levels=LZSS — all decode cleanly with the recovered codec. So the title BG = `unpack_sqz(PRESENT.SQZ)`@0x300 (recovered, 79% = bg; bottom 100%). The earlier "14xx LZ codec to recover" line was WRONG (it's our `unpack_sqz_lzss`). [[pre2-cyxx-reference]] | The intro/menu/title 13h images are produced by: a **bit-stream LZ decompressor at `1030:~14E0` (copy loop `1518`, literal path `14F8`)** — a DIFFERENT codec than the recovered SQZ — that decodes an asset into a source buffer (e.g. `7C24:0`), then `91C0` (`rep movsw` 64000 B) copies it to A000 (mode 13h set at `91A4`, DAC reset to black then faded in via `3034`). The reachable forward image is the MAIN MENU (`91C0` @ ~3.5M ic; has the same logo + "1 START LEVEL 1 / 2 PASSWORD"), which is a SINGLE LZ-decoded asset (logo baked in, decoded by `1518`). The TITLE itself was built BEFORE the only snapshot and the intro does NOT loop back within 25M ic, so the title source/logo can't be captured live from this snapshot. The PRESENT.SQZ@0x300 79% match (via the SQZ codec) is the jungle BG only; the title's real source goes through the 14xx LZ codec (so the SQZ-based asset match was the wrong codec). | (a) recover the `14xx` bit-stream LZ image codec (the intro/menu/title decompressor) — the real foundation; (b) get a title-build witness (capture mid-intro, or cold-boot drive) to ground the title source live at 91A4/91C0; (c) then the base-copy (91C0) + palette-fade (3034) leaves; (d) the logo (single asset vs separate blit — resolve once the codec+witness exist). | scene gap (codec + composited + witness) | OPEN — needs the 14xx LZ codec + a reachable title-build witness |
| 4c | 13h faithful path + title-build WITNESS (2026-06-24) | IMAGE (13h) | — | viewer + codec | **answered + witness obtained** | (1) The faithful viewer has NO 13h path: `play.render_current` routes mode `0x13`/`0x19` straight to `render_vga_rgb(mem, palette)` — a DIRECT read of the VM A000 framebuffer. So the Titus logo + main menu the user sees are ASM-VRAM (a FALLBACK), NOT recovered. (2) Witness `snapshot_pre2_20260624_175530` (pre-menu) DOES reach the title build: the title image is LZ-decoded into the source buffer `[0x2DDA]+0x30` (=`4721:0`) by **`1030:12DE`** (the bulk copy path; lit/back-ref are 14F8/1518) starting ~ic 436126, palette at `[0x2DDA]:0` (768 B 6-bit VGA), then copied to A000 by `91C0` + faded by `3034`. Captured mid-decode (top = jungle bg, bottom still being filled) — the decode is the slow part. | CODEC DONE (unpack_sqz). LOGO DONE 2026-06-24: the title is ONE asset (PRESENT.SQZ) with TWO images — background@0x300 (320x200) + logo-top@0x10300 (320x93) — composited by `91A4` (bg copy) then `9090` (logo-top overlay on A000:0). `pre2.recovered.title_image.render_title_image(unpack_sqz(PRESENT.SQZ))` == the displayed framebuffer **Δ=0 (100%)**; `verify_title_image.py`, `tests/test_title_image.py`. NO separate logo asset / no animation. Remaining: (a) the palette path (asset[:0x300] + the 3034 DAC fade); (b) a 13h FaithfulVisual path (replace the `render_vga_rgb` ASM-VRAM read in play.render_current — NOT done yet per user). | viewer gap (NOT codec; logo done) | PARTIAL — image (bg+logo) recovered Δ=0; palette + 13h faithful path remain |
| 5 | cave-enter curtain (witness `231731`) | transition | none (Δ=0) | transition | **NOT a gap — sub-frame effect already covered** | RESOLVED 2026-06-24 via the `231731` witness: `1030:3054` is the **per-frame double-buffer page-flip** (one call/frame, alternating pages; inner strip copy `1030:309B`, `si+=[0x2DD8]` `di+=[0x2DD6]` — NOT the stale `[0x2DD4]/[0x2DD2]` the old docstring claimed). The center-out strip reveal happens **entirely within one call** (~10 internal vsyncs) — a **sub-frame CRT effect**. By the main-loop frame boundary `1030:6772` the flip is complete, so the committed front page is always a **complete** frame. Confirmed: at a partially-revealed boundary (`disp_black=17%`) the faithful boundary mirror's `render_frame` matches the displayed page **Δ=0%**. | none — curtain is fully covered by existing recovered/hooked code: `panel_copy` (verified full-copy oracle, now `completed_pairs`-capable for an optional sub-frame mirror) + `frame_panel_copy` checkpoint (passthrough → the ASM's vsync timing IS the live reveal) + `render_frame` (reproduces every committed boundary frame). Only fix applied: corrected the stale `panel_copy` docstring/oracle_link to the real `[0x2DD8]`/`[0x2DD6]` pages + `309B` strip address. | — | **CLOSED (2026-06-24) — no faithful curtain leaf needed; reveal is sub-frame, boundary frames Δ=0** |
| 6 | HUD draw (45B8) | GAMEPLAY | none (Δ=0) | HUD | **RESOLVED — checkpoint adapter added** | the HUD leaf had no verifier adapter at its ASM call site (recovered only in mirror + a static golden test + a throwaway probe) | DONE 2026-06-24: added a verify-only checkpoint `pre2/checkpoints/hud.py` at the dynamic-HUD ret (45AB) that diffs the recovered `draw_hud` glyph cells vs the ASM page [0x2DD8] — grounds `draw_hud` + `effective_bonus_mask` against the live oracle (fired 53×, 0 divergences). Runtime *replacement* still deferred (incremental + dual-page + caches [0x6CA0..0x6CA7] → low gain). Superseded probe `verify_hud_layout.py` pruned. | adapter (verifier) | **CLOSED — draw_hud now grounded by a registered checkpoint** |
| 7 | palette fade (021225) | GAMEPLAY | 0 (DAC carries the fade) | palette | **controller ownership (not a visual bug)** | the mirror uses the live DAC, not `fade_palette` | run `fade_palette` in a VisualController so the mirror owns the evolution | ownership cleanup | OPEN (cleanup) |
| 8 | particles `4b8e` (witness `102733` spider threads) | GAMEPLAY | viewport ≤~9 (the point particles) | effects | **RESOLVED — recovered leaf** | the one-shot point particles (`[0x7DE6]`, 20 slots) are drawn + killed by `4B8E` each frame, so the 6772 capture misses them (the "≤10 residual" I'd mis-read as blink phase was the particles) | DONE 2026-06-24: recovered `pre2.recovered.particles.draw_particles` (Δ=0 vs ASM) + `bridge.particles.read_particles` (snapshot at 4B8E entry, pre-kill) + wired into `--faithful` (4B8E capture hook → drawn in the 6772 render). `verify_particles.py` Δ=0; `tests/test_particles.py`. | effect leaf | **CLOSED — particles render faithfully (Δ=0)** |
| 10 | foreground-tile z-order `3721`+`37F7` (witness `110346`, player walks BEHIND a bush) | GAMEPLAY | viewport ≤~290 box (player drawn OVER the foreground tile) | tiles/z-order | **RESOLVED — recovered pass** | flag-`0x40` tiles (`[0x805E+tile]`) must draw IN FRONT of sprites; render_frame had no such concept (player over everything). `3721`/`3732` is the selection (walk active list `[0x4F0A]` stride 0x12; per active sprite id&0x2000 scan a 4-col×3-4-row cell box, grid from tilemap seg `[0x2DDA]`), `37F7` the color-0-keyed transparent blit (gfx at seg`[0x003b]`:`word[0x8167+tile*2]<<7`, phase1 AND ~footprint, phase2 per-plane OR). | DONE 2026-06-24: recovered `pre2.recovered.foreground_tiles.render_foreground_tiles` (selection + blit, Δ=0 vs ASM) + `bridge.read_foreground_state`. The active list is rebuilt each frame, so the faithful path snapshots the state at the 3732 pass entry (like 4b8e) and replays at the 6772 commit. `verify_foreground_tiles.py` Δ=0 (5 passes); `tests/test_foreground_tiles.py`. End-to-end 110346 (moving): base 282 → +fg 18-23 (player behind bush) → +fireflies **0**. | tile/z-order pass | **CLOSED — foreground tiles render over sprites (Δ=0)** |
| 9 | firefly swarm `54AB` (witness `140330` 20 on-screen fireflies; also active in `110346`) | GAMEPLAY | viewport ≤~27 (the swarm pixels) | effects | **RESOLVED — recovered DRAW leaf** | the persistent firefly swarm (`[0x6EA9]`, 20 slots × 8, stride 8, `0x55AA` sentinel = dead) is animated **and** drawn each frame by `54AB`, OR-ing one flicker pixel per slot to the back page — a SEPARATE system from the one-shot `4b8e` points. The 6772 capture re-renders the page without it → fireflies vanish. **Key finding:** the even/odd color-14/15 flicker is set via the EGA **Set/Reset** registers (GC idx 0/1), which `dos_re._ega_wb` does NOT emulate → under the VM oracle every firefly collapses to color **15** (all 4 planes). | DONE 2026-06-24: recovered `pre2.recovered.fireflies.draw_fireflies` (Δ=0 vs the VM oracle) + `bridge.fireflies.read_fireflies` + wired into `--faithful` at the 6772 boundary (slots persist across the frame → no separate hook needed; captured directly). `verify_fireflies.py` Δ=0 over the page; `tests/test_fireflies.py`. The true 14/15 flicker is kept in `firefly_color()` for the enhanced renderer. **UPDATE 2026-06-24: the per-frame RNG ANIMATION is now also recovered + replaced live** — `pre2.recovered.firefly_sim.step_fireflies` (full 54AB: RNG-driven flocking + move + draw) runs as a native replacement (`pre2/checkpoints/fireflies.py` @ 1030:54AB), so the VM skips the interpreted pass (~3000 instr/frame). Byte-exact incl. both SHARED RNGs (26CF/39DF): `verify_firefly_sim.py` 40 frames Δ=0 (slots+seeds+scratch+VRAM); framework verify 0 divergences; `tests/test_firefly_sim.py`. | effect leaf + replacement | **CLOSED — fireflies render faithfully (Δ=0) AND animate via a native 54AB replacement (perf).** |

## Summary
After the frame-boundary fix + the HUD BONUS fix, the **gameplay + iris visual output is byte-exact at
the commit boundary** (viewport HUD Δ=0; the only viewport residual is the negligible object-blink
phase, #2). The remaining faithful-renderer work is the **non-gameplay scene leaves** (#3 SCENE, #4 IMAGE)
+ non-visual cleanup (#6 HUD adapter, #7 palette ownership) + the open #8 particle question. The curtain
(#5) is CLOSED — it is a sub-frame page-flip effect already covered by `panel_copy` + `frame_panel_copy`
+ `render_frame` (boundary frames Δ=0); iris is already reused by `render_visual` (`compose_iris`).

None of the remaining items is a gameplay/object-producer gap — the visual STATE exists and is exported;
what's missing is recovered LEAVES for the non-gameplay scenes/transitions. So the renderer-completion
track stays independent of state ownership (per the milestone separation).

Highest-impact next: a **SCENE leaf** (menu/map, #3) — most witnesses, reuses the already-runtime-replaced
`scroll_blit`/`draw_string` leaves; then IMAGE (#4).
